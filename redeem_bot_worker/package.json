{
  "name": "redeem-bot-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute redeem_bot_db --remote --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute redeem_bot_db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
